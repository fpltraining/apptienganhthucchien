# Địa chỉ backend giữ API key (server/index.mjs).
#
# Bỏ trống: phần "Nói tự do" dùng kịch bản có sẵn, không gọi mạng, không tốn tiền.
# Đặt "/" khi chạy `npm run dev` cùng `npm run server` (Vite tự chuyển tiếp /api).
# Đặt URL đầy đủ khi backend nằm ở máy khác.
VITE_API_BASE=
